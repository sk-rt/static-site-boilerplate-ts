'use strict';

import '../scss/style.scss';
document.addEventListener(
    'DOMContentLoaded',
    () => {
        main();
    },
    false
);

const main = (): void => {
    console.log('hello');
};
